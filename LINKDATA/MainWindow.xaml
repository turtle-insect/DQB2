<Window x:Class="LINKDATA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LINKDATA"
        mc:Ignorable="d"
		WindowStartupLocation="CenterScreen"
        Title="DQB2 LINKDATA Nintendo Switch &amp; Steam" Height="450" Width="800">
	<Window.DataContext>
		<local:ViewModel/>
	</Window.DataContext>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition Width="10*"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition Height="10*"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Label Content="Original"/>
		<TextBox Grid.Column="1" Text="{Binding OriginalFile, UpdateSourceTrigger=PropertyChanged}"/>
		<Button Grid.Column="2" Content="..." Click="ButtonSelectOriginalFile_Click"/>
		<Label Grid.Row="1" Content="Output"/>
		<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}"/>
		<Button Grid.Row="3" Grid.ColumnSpan="3" Content="Decryption" Click="ButtonDecryption_Click"/>
	</Grid>
</Window>
